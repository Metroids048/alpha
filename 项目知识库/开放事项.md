# 开放事项

## 来自 USER_KNOWLEDGE_BASE / 本地

- 核对本项目与主知识库状态标签（OPEN / USER_REPORTED / VERIFIED）
- 补全真实验收证据链接（测试输出、commit）
- 恢复生产 catalog：网页重新登录并导入新会话后，依次执行 python -m alpha_mining platform probe 与 python -m alpha_mining platform catalog-sync；确认三份 .alpha_*_cache.json 均写入后再运行 生成Alpha.py
- 离线候选已生成 100 条；后续若要进入 READY CSV，必须在生产模式完成真实 simulate 与质量门禁，不能把候选队列直接当作待提交列表
- 敏感边界：Brain cookie、提交凭证 — DO_NOT_PUBLISH

## Git

保持主分支可构建可验证
