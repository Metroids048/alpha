# 当前状态

## 最近更新（需定期同步）

- **2026-08-03 目录结构收口**
  - 冻结主线确认为：`生成Alpha.py` → `待提交Alpha列表.csv` → `提交Alpha.py`
  - 根目录过期报告/备份/危险 ps1/`legacy/20260730待清理` 已清理
  - 运维诊断脚本迁入 `tools/ops/`；`auto_alpha_pipeline_rebuilt_v50.py` 仅保留测试回归
  - 幽灵入口 `run_pipeline_*` / 高质量迭代脚本文档引用已对齐

- **2026-08-02 迭代反馈闭环系统上线**（历史；相关独立脚本已退役）
  - 批量 simulate / 反馈 CSV / LLM 改进 prompt 曾作为临时闭环
  - 现由冻结质量工作流承接生成与门槛

- 2026-08-02 FastPlus 门禁已集成至 v50 + domain validator + tests
- 2026-07-16 质量驱动 + 风险门禁 fix: corr、离线缓存 TTL 修复完成
- 2026-07-04 v50 生成管道 + submission 守门员上线

## 活跃工作流（冻结）

1. **生成**：`python 生成Alpha.py`
   - 输出：`待提交Alpha列表.csv`（仅 `READY_TO_SUBMIT`）
2. **提交**：`python 提交Alpha.py`
   - 不重新 simulate；保留台账、Description、SubmissionGuard、dry-run 与确认短语
3. **可选包 CLI**：`python -m alpha_mining --help`
4. **运维诊断**：`tools/ops/`（如 `python tools/ops/wq_auth_check.py`）

## 当前质量状态

历史第1轮验证（40个候选）曾暴露低 Sharpe / 高 Turnover / 低 Fitness；后续以冻结工作流内的 catalog + 质量门槛继续迭代。

## 当前阻塞点

- ✅ 平台访问已打通（session 需定期续期）
- ✅ 冻结生成/提交入口可用
- ⚠️ 候选质量仍需在冻结闭环内持续迭代

## 依赖状态

- FastPlus 工具链：已验证 0.3.1（parser、validator）
- Python env：本地/Agent Python；测试：`pytest -q`（`pytest.ini` → `testpaths=tests`）
- 认证：`.wq_auth_state.json`（本地，不入库）

## 关键文件

### 核心脚本
- `生成Alpha.py` — 唯一生成入口
- `提交Alpha.py` — 唯一提交入口
- `验证提交链路.py` — 链路自检
- `auto_alpha_pipeline_rebuilt_v50.py` — 测试回归单体（非操作员 UX）
- `tools/ops/` — 运维/诊断脚本

### 数据与文档
- `待提交Alpha列表.csv` — 冻结 READY 队列（本地生成，通常不入库）
- `README.md` — 冻结工作流说明
- `docs/02-frozen-solution-v1.0.md` — 冻结方案

## 任务规划

见 `目标与验收标准.md`
