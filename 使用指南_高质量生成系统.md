# 高质量Alpha生成系统 - 使用指南

## 🎯 系统目标

生成符合WorldQuant Brain平台提交标准的高质量Alpha：
- **夏普值** ≥ 1.57
- **Fitness** ≥ 1.0  
- **换手率** 1% - 70%
- **低自相关性**

## 📁 核心文件

### 生成相关
- `生成高质量Alpha.py` - **主生成脚本**（新统一入口）
- `启动高质量Alpha生成.ps1` - 一键启动脚本
- `auto_alpha_pipeline_rebuilt_v50.py` - 核心引擎（勿修改）

### 输出文件
- `高质量Alpha候选.csv` - 生成的候选（供提交脚本使用）
- `alpha_generated_expressions.csv` - 历史表达式（自动维护）

### 提交相关
- `提交Alpha.py` - 提交到平台（保留）

### 清理工具
- `清理旧文件.ps1` - 删除旧版本文件

## 🚀 快速开始

### 第一步：配置环境

确保 `.env` 文件包含正确的凭证：
```bash
WQ_USERNAME=你的用户名
WQ_PASSWORD=你的密码
```

### 第二步：初始化缓存（首次运行）

如果本地没有 `.alpha_datafields_cache.json`，需要先运行：
```powershell
python 提交Alpha.py
```
这会登录一次并缓存字段信息，之后可以完全离线运行。

### 第三步：启动生成

#### 方式1：一键启动（推荐）
```powershell
.\启动高质量Alpha生成.ps1
```

#### 方式2：手动启动
```powershell
python 生成高质量Alpha.py
```

## ⚙️ 高级配置

### 自定义参数

```powershell
# 调整每轮候选数
python 生成高质量Alpha.py --batch-size 100

# 调整轮次间隔
python 生成高质量Alpha.py --interval 90

# 限制运行轮数
python 生成高质量Alpha.py --max-rounds 10

# 自定义输出文件
python 生成高质量Alpha.py --output my_alphas.csv

# 组合使用
python 生成高质量Alpha.py --batch-size 100 --interval 90 --max-rounds 20
```

### 查看所有选项
```powershell
python 生成高质量Alpha.py --help
```

## 📊 理解输出

### 控制台信息

#### 正常运行
```
==========================================================
🔄 第 1 轮生成
==========================================================
✅ 生成: 245 个候选
✅ 转换: 298 个 payload
📊 多样性: 298个候选 → 87种结构骨架 (29.2%)
✅ 新增 156 个候选（总计 156）
⏳ 等待 60 秒...
```

#### 多样性警告
```
⚠️  多样性偏低，系统将自动调整
```
说明：同一结构重复过多，系统会自动调整策略。

#### 连续零新增
```
⚠️  已连续 3 轮新增0个候选
```
说明：生成的候选都是重复的，系统会在5轮后自动归档并重置。

### CSV输出格式

`高质量Alpha候选.csv` 包含以下列：
- **候选ID**: 唯一标识
- **策略家族**: 生成策略类型（如 `arch_explore`, `near_pass_variant`）
- **来源**: 生成来源
- **分数**: 候选评分
- **表达式**: Alpha表达式
- **模拟设置JSON**: 平台提交所需的完整配置
- **精确哈希**: 用于去重
- **参数骨架/字段骨架**: 用于相似度检测

## 🔧 故障排查

### 问题1：缓存错误
```
❌ 生成失败: 403/401/cache相关错误
💡 可能原因: 本地字段缓存缺失
```
**解决方案**：运行 `python 提交Alpha.py` 刷新缓存

### 问题2：持续零新增
```
⚠️  已连续 5 轮新增0个候选
```
**原因**：生成的候选都是已存在的重复项  
**解决方案**：系统会自动归档当前批次并重置去重池

### 问题3：多样性过低
```
📊 多样性: 300个候选 → 15种结构骨架 (5.0%)
⚠️  多样性偏低
```
**解决方案**：已通过配置优化（同结构上限=8，探索型增加），无需手动调整

### 问题4：环境变量未找到
```
❌ 错误: 未找到 WQ_USERNAME 或 WQ_PASSWORD
```
**解决方案**：检查 `.env` 文件是否存在且格式正确

## 💡 优化建议

### 如果想要更高质量
当前配置已经是高质量导向，如果还想提高：
1. 减少 `--batch-size`（如改为100）
2. 增加 `--interval`（如改为120秒）
3. 修改脚本中的 `min_sharpe_threshold`（如改为1.70）

### 如果想要更多候选
```powershell
python 生成高质量Alpha.py --batch-size 200 --interval 30
```

### 如果想要更多样性
当前配置已经是多样性导向：
- ✅ 探索型预设（`diverse_exploration`）
- ✅ 近通过变异仅15%
- ✅ 同结构上限8次
- ✅ 25%低竞争字段
- ✅ 跨字段变体启用

## 📈 监控运行

### 查看生成进度
```powershell
# 实时查看CSV行数
Get-Content "高质量Alpha候选.csv" | Measure-Object -Line

# 查看最新生成的候选
Get-Content "高质量Alpha候选.csv" -Tail 10
```

### 统计多样性
```powershell
# PowerShell统计唯一字段骨架数
$csv = Import-Csv "高质量Alpha候选.csv" -Encoding UTF8
$csv | Select-Object -Property "字段骨架" -Unique | Measure-Object
```

## 🧹 清理旧文件

如果你的目录还有旧版本文件，运行：
```powershell
.\清理旧文件.ps1
```
系统会提示确认后删除所有旧版本文件。

## 📚 文件对比

### 旧系统 vs 新系统

| 项目 | 旧系统 | 新系统 |
|------|--------|--------|
| 入口脚本 | `生成Alpha.py`, `生成Alpha_v2.py` | **`生成高质量Alpha.py`** |
| 启动脚本 | `启动高质量生成.ps1` | **`启动高质量Alpha生成.ps1`** |
| 输出文件 | `待提交Alpha列表.csv` | **`高质量Alpha候选.csv`** |
| 夏普门槛 | 1.25 | **1.57** |
| 每轮候选 | 300 | **150** |
| 近通过占比 | 40% | **15%** |
| 同结构上限 | 16 | **8** |
| 探索导向 | 关闭 | **启用** |

## ⚠️ 重要提示

1. **不要修改核心引擎**：`auto_alpha_pipeline_rebuilt_v50.py` 是经过验证的稳定版本
2. **保持离线模式**：已配置离线运行，无需修改
3. **定期检查缓存**：如果长时间未登录，缓存可能过期
4. **监控输出质量**：定期检查生成的候选是否满足1.57门槛
5. **备份重要输出**：`高质量Alpha候选.csv` 会在连续5轮零新增后自动归档

## 📞 问题反馈

如遇到其他问题，请检查：
1. `.env` 文件配置是否正确
2. `.alpha_datafields_cache.json` 是否存在
3. 网络连接是否正常（首次登录需要）
4. Python环境是否完整

---

**最后更新**: 2026-08-02  
**系统版本**: v1.0 (基于 v50.7 引擎)
