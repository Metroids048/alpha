# 离线生成死循环问题修复报告

## 📋 问题现象

系统在第69-95轮连续出现：
- ✅ 每轮生成 228 个候选
- ✅ 多样性 33.3%（76种结构骨架）
- ❌ **新增 0 个候选**（连续5轮后自动归档重置）
- ❌ 归档后第1轮新增228个，然后又是连续4-5轮新增0

## 🔍 根本原因分析

### 1. 日志证据
```
[history] token_rows=830 (kept 100% of 830 unique)
[generate] raw=6305 kept=221 
top_rejects: structure_budget_exceeded:4989, history_skeleton_seen:670
```

### 2. 问题拆解

#### 问题A：历史反馈错误
**位置**：第351行 `feed_back_to_registry(registry_path, payloads, ...)`

**错误**：
- 原代码将**所有228个payloads**写入历史注册表
- 但这228个全部是**重复的**（exact_hash已存在）
- 下一轮引擎读取历史时，读到的还是相同的830行
- **无法打破离线确定性循环**

**修复**：
```python
# 修复前：写入所有payloads（包括重复的）
feed_back_to_registry(registry_path, payloads, run_salt, generation_time)

# 修复后：只写入新增的payloads
new_payloads = [p for p in payloads if p.get("meta", {}).get("exact_hash") not in existing_hashes]
payloads_to_feedback = new_payloads if new_payloads else payloads[:2]
feed_back_to_registry(registry_path, payloads_to_feedback, run_salt, generation_time)
```

#### 问题B：骨架去重过严
**位置**：引擎配置中的 `block_history_skeleton_always`

**错误**：
- `history_skeleton_seen:670` - 670个候选因骨架重复被拒绝
- `block_history_skeleton_always = True`（默认值）阻止了所有历史骨架的变体
- **即使参数不同，只要骨架相同就被拒绝**
- 导致生成空间快速耗尽

**修复**：
```python
# 修复前：严格骨架去重
cfg.max_history_similarity = 0.82
cfg.prescreen_max_history_similarity = 0.78
cfg.block_history_skeleton_always = True  # 默认值

# 修复后：放宽骨架去重
cfg.max_history_similarity = 0.85  # 0.82 -> 0.85
cfg.prescreen_max_history_similarity = 0.82  # 0.78 -> 0.82
cfg.block_history_skeleton_always = False  # 禁用骨架硬性去重
cfg.template_skip_history_skeleton = False
```

## ✅ 修复方案

### 修改1：历史反馈逻辑
```python
# 文件：生成高质量Alpha.py
# 行：354-360

# 【关键修复】只反馈新增的候选到历史注册表，避免重复写入导致历史爆炸
# 如果全部是重复的，至少写入1-2个作为"尝试过的证据"，促使引擎下次调整策略
new_payloads = [p for p in payloads if p.get("meta", {}).get("exact_hash") not in existing_hashes]
payloads_to_feedback = new_payloads if new_payloads else payloads[:2]
feed_back_to_registry(registry_path, payloads_to_feedback, run_salt, generation_time)

if not new_payloads and payloads:
    print(f"💡 提示: 本轮228个候选全部重复，已记录2个作为历史证据，引擎下次将调整策略")
```

### 修改2：放宽骨架去重
```python
# 文件：生成高质量Alpha.py
# 行：190-200（_make_high_quality_pipeline函数内）

# 【关键修复】历史相似度：放宽骨架去重，允许更多结构变体
cfg.max_history_similarity = 0.85  # 0.82 -> 0.85
cfg.prescreen_max_history_similarity = 0.82  # 0.78 -> 0.82
cfg.prescreen_intrabatch_similarity = 0.82  # 0.78 -> 0.82

# 【关键修复】禁用骨架历史去重（block_history_skeleton_always）
# 这是导致"history_skeleton_seen:670"大量拒绝的根源
cfg.block_history_skeleton_always = False  # True -> False
cfg.template_skip_history_skeleton = False  # True -> False
```

## 📊 预期效果

### 修复前
```
第69轮：新增0（历史830行，骨架完全重复）
第70轮：新增0（历史830行，读取相同数据）
第71轮：新增0（历史830行，读取相同数据）
第72轮：新增0（历史830行，读取相同数据）
第73轮：归档重置 -> 新增228（去重池清空）
第74轮：新增0（历史830+228=1058行，但还是相同骨架）
...循环往复
```

### 修复后
```
第N轮：新增150（历史830行）
第N+1轮：新增120（历史830+150=980行，新骨架加入）
第N+2轮：新增100（历史980+120=1100行，逐渐多样化）
第N+3轮：新增80（历史1100+100=1200行，空间扩大）
...逐步填充生成空间
```

## 🎯 为什么这样修复有效

### 1. 历史反馈修复
- ✅ **只写入新候选**：避免重复写入，历史注册表真实增长
- ✅ **打破确定性**：每轮历史都有新内容，引擎被迫尝试新组合
- ✅ **证据记录**：即使全重复，也记录2个作为"尝试过的证据"

### 2. 骨架去重修复
- ✅ **扩大生成空间**：允许相同骨架的不同参数组合
- ✅ **减少拒绝率**：`history_skeleton_seen` 从670降低
- ✅ **保持质量**：通过 `max_history_similarity` 控制精确相似度

### 3. 两者协同
- 历史反馈 → 增加历史多样性 → 引擎学习新模式
- 骨架放宽 → 允许更多变体 → 不会因"骨架见过"被一刀切拒绝
- 质量门槛 → 夏普1.57保持 → 只是扩大搜索空间，不降低标准

## ⚠️ 权衡与注意事项

### 放宽骨架去重的风险
**风险**：可能生成更多"骨架相同但参数微调"的候选
**缓解**：
- ✅ 保持 `max_same_shape_per_run = 8`（同结构上限）
- ✅ 保持 `behavior_similarity_cap = 0.75`（行为多样性）
- ✅ 保持夏普门槛 1.57（质量过滤）

### 历史注册表增长
**影响**：历史注册表会逐步增长（830 -> 980 -> 1200...）
**缓解**：
- ✅ 只写入新候选，增长速度可控
- ✅ v50引擎有 `similarity_history_max_token_rows = 18000` 上限
- ✅ 达到上限后自动采样（reservoir sampling）

## 🔧 测试建议

### 1. 观察指标
```bash
# 运行修复后的脚本
python 生成高质量Alpha.py --max-rounds 10

# 观察以下指标：
# - 每轮新增数量（应该>0）
# - history_skeleton_seen 拒绝数（应该降低）
# - 历史注册表行数增长（应该逐步增加）
# - 多样性比例（应该保持30%+）
```

### 2. 成功标志
- ✅ 连续10轮没有"新增0"
- ✅ `history_skeleton_seen` < 300（从670降低）
- ✅ 历史注册表每轮增加50-150行
- ✅ 多样性保持在30%-40%

### 3. 失败信号
- ❌ 仍然出现连续5轮新增0
- ❌ `history_skeleton_seen` 仍然>600
- ❌ 历史注册表停止增长

## 📚 相关配置

### 完整配置对比

| 参数 | 修复前 | 修复后 | 说明 |
|------|--------|--------|------|
| `max_history_similarity` | 0.82 | **0.85** | 放宽精确哈希相似度 |
| `prescreen_max_history_similarity` | 0.78 | **0.82** | 放宽预筛选相似度 |
| `prescreen_intrabatch_similarity` | 0.78 | **0.82** | 放宽批次内相似度 |
| `block_history_skeleton_always` | True | **False** | 禁用骨架硬性去重 |
| `template_skip_history_skeleton` | True | **False** | 模板也允许骨架重复 |
| 历史反馈逻辑 | 写入所有 | **只写入新增** | 关键修复 |

### 不变的质量门槛

| 参数 | 值 | 说明 |
|------|------|------|
| `min_sharpe_threshold` | **1.57** | 夏普门槛（用户要求） |
| `min_fitness_threshold` | **1.0** | Fitness门槛 |
| `min_turnover_threshold` | **0.01** | 最低换手率 |
| `max_turnover_threshold` | **0.70** | 最高换手率 |
| `max_same_shape_per_run` | **8** | 同结构上限 |
| `behavior_similarity_cap` | **0.75** | 行为多样性 |

## ✅ 结论

**问题**：离线生成陷入确定性死循环（连续新增0）

**根源**：
1. 历史反馈逻辑错误（写入重复候选）
2. 骨架去重过严（生成空间耗尽）

**修复**：
1. 只反馈新增候选到历史
2. 放宽骨架去重限制
3. 保持质量门槛不变

**效果**：
- ✅ 打破离线确定性循环
- ✅ 扩大生成空间
- ✅ 保持高质量门槛（夏普≥1.57）

---

**修复时间**：2026-08-02  
**文件**：`生成高质量Alpha.py`  
**状态**：✅ 已修复，待测试验证
