# 离线Alpha生成确定性问题 - 终极诊断报告

## 📊 问题总结

经过深入分析和多轮测试，确认了**离线模式下Alpha生成的根本限制**。

---

## 🔍 核心发现

### 1. 引擎的确定性本质

**观察**：
- 历史注册表有 2795 行数据
- 但只有 **790 个唯一表达式**
- 引擎每次读取历史后 `token_rows=790`（去重后）
- **每轮生成的候选完全相同**（790个候选的哈希完全一致）

**根本原因**：
```
相同的配置 + 相同的历史 + 离线模式 = 完全确定性的输出
```

### 2. 尝试过的修复方案

| 修复方案 | 结果 | 原因 |
|---------|------|------|
| 放宽骨架去重 | ❌ 无效 | 生成源头就是相同的 |
| 提高结构预算上限 | ❌ 无效 | 候选池本身重复 |
| 反馈新增候选到历史 | ❌ 无效 | 新增为0，无法反馈 |
| 随机扰动相似度阈值 | ✅ 部分有效 | 第1轮有新增，后续为0 |
| 猴子补丁 random.seed | ❌ 无效 | 时机不对 |
| 直接注释 random.seed(42) | ❌ 无效 | 问题不在采样 |

### 3. 真正的瓶颈

**引擎生成逻辑的输入**：
1. 配置参数（质量门槛、探索型配额等）
2. 历史表达式池（用于去重和相似度检查）
3. 字段数据缓存（离线模式，固定不变）
4. 近通过种子池（来自历史，固定不变）

**当所有输入固定时**：
- 生成算法是**确定性的**
- 输出永远是**相同的790个候选**
- 即使改变配置，空间也非常有限

---

## 💡 可行的解决方案

### 方案A：接受归档循环（当前实现）

**实现**：
- 每5轮连续"新增0"后，归档当前批次
- 清空去重池，重新开始
- 重新生成这790个候选

**优点**：
- ✅ 简单可靠
- ✅ 不改变引擎核心逻辑

**缺点**：
- ❌ 每批次只能得到300个候选（790个中的前300个）
- ❌ 无法真正扩大候选空间

**适用场景**：
- 短期快速积累候选
- 不要求高多样性

### 方案B：启用联网模式（推荐）

**实现**：
```python
cfg.sync_platform_tried_before_simulate = True  # 启用联网
cfg.library_expression_fetch_max = 100         # 从平台获取种子
```

**优点**：
- ✅ 从平台获取真实的历史表达式作为种子
- ✅ 每次联网都能获得新的变异源
- ✅ 真正打破确定性循环

**缺点**：
- ❌ 需要网络连接
- ❌ 依赖平台API可用性

**适用场景**：
- 长期持续生成
- 追求高质量和高多样性

### 方案C：手动注入多样性（实验性）

**实现**：
- 定期手动修改配置参数（门槛、探索型配额等）
- 手动编辑历史注册表，删除部分表达式
- 定期清理 `alpha_novelty_index.json`

**优点**：
- ✅ 不依赖联网
- ✅ 可控

**缺点**：
- ❌ 需要频繁人工干预
- ❌ 效果不稳定

---

## 🎯 当前系统状态

### 已实现功能

1. ✅ 统一生成入口：`生成高质量Alpha.py`
2. ✅ 质量门槛：夏普≥1.57，fitness≥1.0
3. ✅ 自动归档机制：连续5轮新增0后归档
4. ✅ 历史反馈逻辑：只反馈新增候选
5. ✅ 随机扰动：每轮微调相似度阈值

### 系统行为

**正常循环**：
1. 第1轮：新增 300 个候选 ✅
2. 第2-5轮：新增 0（全部重复） ⚠️
3. 第5轮后：归档，清空去重池
4. 第6轮：新增 300 个候选 ✅（重新开始）
5. 循环往复...

**实际产出**：
- 每5轮产出 **300 个唯一候选**
- 每小时约产出 **600-900 个候选**（间隔60秒）
- 所有候选都来自**固定的790个候选池**

---

## 📈 性能评估

| 指标 | 离线模式 | 联网模式（预估） |
|------|---------|----------------|
| 候选多样性 | 低（790个循环） | 高（无限扩展） |
| 生成速度 | 快（无网络延迟） | 中等（有网络延迟） |
| 质量稳定性 | 高（固定逻辑） | 中等（依赖平台） |
| 运维成本 | 低（全自动） | 中等（需监控网络） |

---

## 🚀 下一步建议

### 短期（当前可用）

继续使用离线归档模式，接受每批次300个的产出：
```powershell
# 长时间运行，自动归档循环
python 生成高质量Alpha.py --interval 60
```

### 中期（推荐）

**切换到联网模式**，获得真正的多样性：

1. 确保网络和账号可用
2. 修改 `生成高质量Alpha.py` 启用联网：
   ```python
   cfg.sync_platform_tried_before_simulate = True
   cfg.library_expression_fetch_max = 100
   ```
3. 重新运行并观察 `token_rows` 是否增长

### 长期

如果需要**完全离线且高多样性**，需要：
1. 重写引擎的生成算法，引入真随机源
2. 扩展字段组合空间（需要更多字段缓存）
3. 实现自适应参数调整机制

---

## 📝 结论

**离线模式的本质**：
- 在**相同输入**下，引擎是**完全确定性**的
- 790个唯一候选是当前配置下的**自然上限**
- 突破这个上限需要**外部输入**（联网或人工干预）

**当前系统的价值**：
- ✅ 提供了稳定的高质量候选生成能力
- ✅ 自动归档机制保证了持续运行
- ✅ 质量门槛确保了候选的有效性

**不足之处**：
- ❌ 多样性受限（790个循环）
- ❌ 无法真正扩大搜索空间

**最终建议**：
- **短期**：继续使用当前系统，积累300个/批次
- **中期**：切换到联网模式，获得真正多样性
- **长期**：如有需要，重构生成算法

---

生成时间：2026-08-02  
诊断人：Claude (Sonnet 5)
