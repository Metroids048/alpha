# 🔧 紧急修复：离线生成死循环（2026-08-02）

## 问题
系统在第69-95轮陷入死循环：
- ✅ 每轮生成228个候选
- ❌ **新增0个**（连续5轮后归档重置）
- ❌ 归档后新增228个，然后又是连续0

## 根本原因

### 原因1：历史反馈错误
```python
# 错误：写入所有228个payloads（包括重复的）
feed_back_to_registry(registry_path, payloads, ...)

# 结果：历史注册表没有真实增长，每轮读到相同的830行
```

### 原因2：骨架去重过严
```
[generate] top_rejects: history_skeleton_seen:670

# 670个候选因"骨架在历史中见过"被拒绝
# block_history_skeleton_always=True 阻止了所有历史骨架的变体
# 生成空间快速耗尽
```

## 修复方案

### 修复1：只反馈新增候选
```python
# 只写入新候选到历史
new_payloads = [p for p in payloads if p.get("meta", {}).get("exact_hash") not in existing_hashes]
payloads_to_feedback = new_payloads if new_payloads else payloads[:2]
feed_back_to_registry(registry_path, payloads_to_feedback, run_salt, generation_time)
```

### 修复2：放宽骨架去重
```python
# 扩大生成空间
cfg.max_history_similarity = 0.85  # 0.82 -> 0.85
cfg.prescreen_max_history_similarity = 0.82  # 0.78 -> 0.82
cfg.block_history_skeleton_always = False  # True -> False（关键）
cfg.template_skip_history_skeleton = False
```

## 测试验证

运行修复后的脚本：
```powershell
python 生成高质量Alpha.py --max-rounds 10
```

**成功标志**：
- ✅ 连续10轮没有"新增0"
- ✅ `history_skeleton_seen` < 300（从670降低）
- ✅ 历史注册表每轮增加50-150行

**失败信号**：
- ❌ 仍然连续5轮新增0
- ❌ `history_skeleton_seen` 仍然>600

## 权衡说明

**放宽骨架去重的风险**：可能生成更多"骨架相同但参数微调"的候选

**缓解措施**：
- ✅ 保持 `max_same_shape_per_run = 8`（同结构上限）
- ✅ 保持 `behavior_similarity_cap = 0.75`（行为多样性）
- ✅ 保持夏普门槛 1.57（质量过滤）

## 质量保证

**不变的门槛**：
- 夏普≥1.57 ✅
- Fitness≥1.0 ✅
- 换手率1%-70% ✅
- 同结构上限=8 ✅

---

**状态**: ✅ 已修复  
**测试**: ⏳ 待验证  
**文件**: `生成高质量Alpha.py`
